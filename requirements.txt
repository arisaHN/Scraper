# HTTP
requests==2.32.5
camoufox[geoip]==0.4.11
# Pinned explicitly: the Dockerfile's coreBundle.js sed patches target this exact
# version's bundled driver file and must be re-verified if it's bumped.
playwright==1.60.0

# Database
SQLAlchemy==2.0.36
psycopg2-binary==2.9.9
alembic==1.13.2

# HTML parsing (ditano.com Judge.me review widget)
beautifulsoup4==4.14.3
lxml==6.1.0

# Config & utilities
python-dotenv==1.0.1
tenacity==8.3.0
click==8.1.8
python-dateutil==2.9.0.post0
